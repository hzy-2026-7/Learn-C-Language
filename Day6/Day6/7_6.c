/*

2026年7月6号 晚
状态不好，精疲力尽

一、自增自减运算符
1、++ 变量中的值+1
2、-- 变量中的值-1
注意，++和--既可以放在变量的前面，也可以放在变量的后面
1）单独使用：只要是单独写一行，结果就是一样的
2）参与计算|考选择题：后++，先用后加；先++，先加后用
Windows 前缀优先于后缀
Mac、Linux 前缀与后缀优先级一致，从左至右依次计算

二、赋值运算符
1、= 直接赋值
2、+= -= *= /= %=

三、关系运算符 | 比较
==  !=  >  >=  <  <=
成立 1（真）
不成立 0（假）

*/

#include <stdio.h>
int main()
{
	return 0;
}